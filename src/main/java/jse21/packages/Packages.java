package jse21.packages; // Essa linha indica o nome do pacote em que essa classe esta

public class Packages {

    /**
     * Um pacote e um agrupamento de tipos relacionados que prove controle de acesso e o gerenciamento de namespaces.
     *
     * Os tipos agrupados podem ser classes, interfaces, enums e annotations.
     *
     * Os pacotes garantem que os tipos sejam encontrados mais facilmente, uma vez que estao dividos por
     * assuntos/funcionalidades, evitando assim conflitos de tipos com o mesmo nome
     */
}
