package jsez15.Packages;

public class UsingPackages {

	Packages p1 = new Packages(); // If the 'import' statement is not declared, the class must be referred by the fully name
	
	// Instead this and to be more clearly, use 'import' and than just the class name
	
	Packages p2 = new Packages();
	
}
